# 提交说明

这是我本次 `Assignment 2 - Word2Vec` 的最终提交版本。

我在代码部分完成了 `skip-gram + negative sampling` 的实现，并生成了可直接查看的完成版 notebook、训练损失曲线、二维可视化图和中文实验报告。按照助教在群里的最新说明，本次代码作业只需要**任意一组**超参数达到可接受的 analogy accuracy 即可；我最终采用的最佳配置是 `set1`，它在 `top-5` 标准下达到了 `0.0159` 的准确率。

压缩包中保留了我用于复现结果的核心代码、已执行 notebook、图表、结果汇总文件和中文 PDF 报告。
